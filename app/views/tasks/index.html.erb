<div class = "container">
  <h1>My Tasks 📝</h1>
  <ul>
    <% @tasks.each do |task| %>
      <li><%=link_to task.title, task_path(task.id) %></li>
    <% end %>
  </ul>
  <p><%=link_to "Create a new task", task_path(:new) %></p>
</div>
